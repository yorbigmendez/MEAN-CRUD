import { DateFormatValidationDirective } from './date-format-validation.directive';

describe('DateFormatValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormatValidationDirective();
    expect(directive).toBeTruthy();
  });
});
