import { PhoneValidationDirective } from './phone-validation.directive';

describe('PhoneValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneValidationDirective();
    expect(directive).toBeTruthy();
  });
});
